projection;
strict ( 2 );
use draft;

define behavior for zchc_sd_sample //alias <alias_name>
{
//  use create;
  use update;
  use delete;

  use action to_text_app;
  use action create_text;
  use action save;
  use action to_app;
  use action file_name;
  use action file_name2;
  use action save_excel;
  use action save_json;
  use action save_xml;
  use action save_text;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}